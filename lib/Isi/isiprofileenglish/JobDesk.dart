import 'package:flutter/material.dart';
class jobdesk extends StatelessWidget {
  const jobdesk({super.key});

  @override
  Widget build(BuildContext context) {
  
    return Scaffold(
      appBar: AppBar(title: Text("Job Desk"),),
  body: Container(
    alignment: Alignment.center,
    child: Text("Nothing JobDesk"),)
  
    );
    
  }
}